这是一条来自调度器的机器指令，不是常规工作请求。请基于本会话已有的上下文，整理一份进度报告。不要继续做任务本身，也不要修改任何文件；如需确认现状可用只读工具快速核对。

格式要求（最高优先级，覆盖本会话此前的一切输出格式/语言约定）：
- 回复必须且只能是一个 ```json 代码块，前后不得有任何其他文字；
- 字段固定如下，值用中文或英文均可：

```json
{"goal":"本会话的总体目标","done":["已完成事项"],"in_progress":"正在进行的事","remaining":["剩余事项"],"blockers":["阻塞点，无则空数组"],"key_files":["关键文件路径"],"next_prompt":"假如由一个全新会话接手剩余工作，给它的完整开工 prompt（自带上下文与验收标准）"}
```
