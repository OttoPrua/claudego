你现在**独立**地对下面这个任务给出你自己的结论。这是一次多引擎交叉验证：另一个独立引擎会对同一任务独立作答，稍后两份结论交叉比对、互相查漏。因此此刻请**只依据任务本身与第一性原理推理**——不要揣测另一引擎会怎么答、不要迎合任何预设立场、不要为求一致而收敛。你的独立性正是这次验证的价值来源。

**独立性纪律（必须遵守）**：只依据下面「任务」一节的内容（及其点名的项目源码/契约）作答；**不要**去读取或搜索任务未点名的编排/状态文件（如 `~/.claudego/`、`tasks/`、`logs/`、`crosscheck/` 等目录），**不要**试图寻找另一引擎的结论——那会污染你的独立判断，使这次交叉验证失去意义。

## 方法纪律（必须遵守）
1. **第一性原理**：从最基本的事实、约束、契约与目标出发重新推导，不套用惯例、不投机抽象、不为凑结论而假设。每个承重结论都要能追溯到具体证据（源码/契约文档/实测/上游消费方视角），并为每个承重假设标注**证据等级**：源码证实 / 实测 / 合理推断 / 未验证。
2. **对抗式自审**：得出初步结论后，主动扮演反方**证伪自己**——构造至少一个能推翻该结论的具体场景（边界输入 / 契约消费方视角 / 并发时序 / 崩溃点 / 向后兼容）。击不破的才保留；击破的据实修正结论。绝不用"测试全绿""看起来对"当通过理由。
3. 覆盖维度：正确性、边界条件、契约面语义（是否悄改了既有接口/枚举/路径/schema）、并发与状态、安全性、可维护性。全程严格区分「已证事实」与「你的判断」。

## 任务
{{TASK}}

## 输出（务必自包含，供另一引擎交叉比对）
先写出完整的分析与证伪过程，最后用一个 ```json 代码块给出结构化结论（务必是合法 JSON）：
{"verdict":"你的判定（自然语言，几个词到一句）","key_findings":["每条自包含：位置(路径:行号若适用)+现象+依据"],"assumptions":["承重假设 + 证据等级"],"open_questions":["仍需进一步确认/无法从现有材料裁定的项"],"confidence":"high|medium|low","summary":"一句话结论"}
