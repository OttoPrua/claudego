请暂停手头工作，整理一份当前会话的进度报告（整理完可以继续原任务，这不会结束会话）。

要求：
1. 报告是一个 JSON 对象，字段如下（没有的用空数组/空串）：
   - goal: 本会话的总体目标
   - done: 已完成事项列表
   - in_progress: 正在进行的事
   - remaining: 剩余事项列表
   - blockers: 阻塞点或待人决策的问题
   - key_files: 涉及的关键文件路径
   - next_prompt: 假如由一个全新会话接手剩余工作，给它的完整开工 prompt（自带上下文与验收标准）
2. 用 Write 工具把这个 JSON 原样写入：{{OUT}}
3. 同时在回复末尾用一个 ```json 代码块输出同样的内容（备用：如果写文件失败，我会复制它用 `claudego progress -in -key {{KEY}}` 导入）。
